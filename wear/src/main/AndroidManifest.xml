<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:name="android.hardware.type.watch" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.NFC" />
    <uses-permission android:name="com.google.android.permission.PROVIDE_BACKGROUND" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Required since API 34 for any service declaring foregroundServiceType="connectedDevice" -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <!-- Signature|privileged permissions only granted on the privileged/DemiGod build variant
         (system-signed or installed as a system app) - a non-privileged build simply never gets
         them, which is expected and harmless, so the ProtectedPermissions warning is suppressed
         here rather than removing permissions this build variant genuinely needs. -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_PRIVILEGED"
        tools:ignore="ProtectedPermissions" />
    <uses-permission
        android:name="android.permission.INTERACT_ACROSS_USERS_FULL"
        tools:ignore="ProtectedPermissions" />
    <uses-permission
        android:name="android.permission.INSTALL_PACKAGES"
        tools:ignore="ProtectedPermissions" />
    <!-- lets a non-privileged (non-DemiGod) build self-update via PackageInstaller.Session
         with a one-tap system confirmation dialog, no ADB/root needed -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission
        android:name="android.permission.DELETE_PACKAGES"
        tools:ignore="ProtectedPermissions" />
    <uses-permission
        android:name="android.permission.CHANGE_COMPONENT_ENABLED_STATE"
        tools:ignore="ProtectedPermissions" />
    <uses-permission
        android:name="android.permission.MANAGE_DEVICE_ADMINS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission
        android:name="android.permission.DEVICE_POWER"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <!-- Unlike the phone app (targetSdk 26, where the legacy location-based BLE permission model
         still applies), this module targets 34 - Android requires these to be held before any
         BLE scan/GATT connect will succeed. LocationPermissionActivity already requests both at
         runtime on API 31+, but a runtime request for a permission absent from the manifest is
         silently denied, so without this declaration the watch's own G5 collector can never
         actually talk to the transmitter even after being handed collection duty.
         usesPermissionFlags="neverForLocation" on BLUETOOTH_SCAN: since Android 12, BLE scan
         results are silently withheld system-wide whenever the device's Location toggle is off,
         regardless of app permissions - confirmed on-device via "BluetoothUtils: Permission
         denial: Location is off." right as Ob1G5CollectionService's scan started, with zero
         results ever returned and the transmitter's MAC never learned. This flag tells the OS the
         app never derives location from scan results (true here - only used to find the paired
         transmitter's advertisement), so it stops gating scans on the Location toggle. -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- Declaration that this app is usable on phones with Bluetooth Low Energy. -->
    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="false" />

    <uses-permission-sdk-23 android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission-sdk-23 android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="com.google.android.gms.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="com.google.android.wearable.permission.BIND_COMPLICATION_PROVIDER" />
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Required from API 33 (Android 13) - without it, notifications are silently
         dropped when targetSdk >= 33, even though this manifest requests the permission. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Matches app/src/main/AndroidManifest.xml - needed for AlarmManager.setAlarmClock()
         (DexCollectionService's failover/retry timers) on API 33+, else SecurityException. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
        android:name=".xdrip"
        android:allowBackup="false"
        android:icon="@drawable/ic_icon"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.DeviceDefault"
        tools:replace="android:allowBackup">

        <!-- If you want your app to run on pre-22, then set required to false -->
        <uses-library
            android:name="com.google.android.wearable"
            android:required="false" />

        <!-- Declares this watch app can run without a connected/paired companion phone -
             genuinely true here, since DexCollectionService can independently collect from the
             sensor via BLE when the phone is unreachable (see the staleness-based failover in
             Notifications.checkPhoneDataStalenessFailover). -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <meta-data
            android:name="AA_DB_NAME"
            android:value="DexDrip.db" />
        <meta-data
            android:name="AA_DB_VERSION"
            android:value="41" />
        <meta-data
            android:name="AA_MODELS"
            android:value="com.eveningoutpost.dexdrip.models.ActiveBgAlert,com.eveningoutpost.dexdrip.models.ActiveBluetoothDevice,com.eveningoutpost.dexdrip.models.AlertType,com.eveningoutpost.dexdrip.models.BgReading,com.eveningoutpost.dexdrip.models.Calibration,com.eveningoutpost.dexdrip.models.CalibrationRequest,com.eveningoutpost.dexdrip.models.PebbleMovement,com.eveningoutpost.dexdrip.models.HeartRate,com.eveningoutpost.dexdrip.models.BloodTest,com.eveningoutpost.dexdrip.models.Sensor,com.eveningoutpost.dexdrip.models.TransmitterData,com.eveningoutpost.dexdrip.models.Treatments,com.eveningoutpost.dexdrip.models.UserError,com.eveningoutpost.dexdrip.models.UserNotification,com.eveningoutpost.dexdrip.sharemodels.models,com.eveningoutpost.dexdrip.utilitymodels.BgSendQueue,com.eveningoutpost.dexdrip.utilitymodels.CalibrationSendQueue,com.eveningoutpost.dexdrip.utilitymodels.SensorSendQueue,com.eveningoutpost.dexdrip.models.LibreData,com.eveningoutpost.dexdrip.models.LibreBlock" />

        <meta-data
            tools:replace="android:value"
            android:name="android.support.VERSION"
            android:value="26.1.0" />

        <!--
        ActiveAndroid's Model.save() calls ContentResolver.notifyChange() against a
        content://${applicationId} URI - without this provider declared, that call throws a
        RemoteException on targetSdk 34, crashing the whole process (observed crashing
        ListenerService.onMessageReceived -> saveIncomingBgHistory -> Sensor.create/BgReading.save).
        -->
        <provider
            android:name="com.activeandroid.content.ContentProvider"
            android:authorities="${applicationId}"
            android:enabled="true"
            android:exported="false" />

        <service
            android:name=".Home"
            android:allowEmbedded="true"
            android:exported="true"
            android:label="@string/label_xdrip"
            android:permission="android.permission.BIND_WALLPAPER">
            <meta-data
                android:name="android.service.wallpaper"
                android:resource="@xml/watch_face" />
            <meta-data
                android:name="com.google.android.wearable.watchface.preview"
                android:resource="@drawable/watchface_graph" />

            <intent-filter>
                <action android:name="android.service.wallpaper.WallpaperService" />

                <category android:name="com.google.android.wearable.watchface.category.WATCH_FACE" />
            </intent-filter>
        </service>
        <service
            android:name=".LargeHome"
            android:allowEmbedded="true"
            android:exported="true"
            android:label="@string/label_xdrip_large"
            android:permission="android.permission.BIND_WALLPAPER">
            <meta-data
                android:name="android.service.wallpaper"
                android:resource="@xml/watch_face" />
            <meta-data
                android:name="com.google.android.wearable.watchface.preview"
                android:resource="@drawable/watchface_dark" />

            <intent-filter>
                <action android:name="android.service.wallpaper.WallpaperService" />

                <category android:name="com.google.android.wearable.watchface.category.WATCH_FACE" />
            </intent-filter>
        </service>
        <service
            android:name=".BIGChart"
            android:allowEmbedded="true"
            android:exported="true"
            android:label="@string/label_xdrip_big_chart"
            android:permission="android.permission.BIND_WALLPAPER">
            <meta-data
                android:name="android.service.wallpaper"
                android:resource="@xml/watch_face" />
            <meta-data
                android:name="com.google.android.wearable.watchface.preview"
                android:resource="@drawable/watchface_bigchart" />

            <intent-filter>
                <action android:name="android.service.wallpaper.WallpaperService" />

                <category android:name="com.google.android.wearable.watchface.category.WATCH_FACE" />
            </intent-filter>
        </service>
        <service
            android:name=".CircleWatchface"
            android:allowEmbedded="true"
            android:exported="true"
            android:label="@string/label_xdrip_circle"
            android:permission="android.permission.BIND_WALLPAPER">
            <meta-data
                android:name="android.service.wallpaper"
                android:resource="@xml/watch_face" />
            <meta-data
                android:name="com.google.android.wearable.watchface.preview"
                android:resource="@drawable/watchface_circle" />

            <intent-filter>
                <action android:name="android.service.wallpaper.WallpaperService" />

                <category android:name="com.google.android.wearable.watchface.category.WATCH_FACE" />
            </intent-filter>
        </service>
        <service
            android:name=".ListenerService"
            android:exported="true">
            <!--
            BIND_LISTENER was officially deprecated in 2016 in favor of fine-grained per-action
            intent filters (CAPABILITY_CHANGED/DATA_CHANGED/MESSAGE_RECEIVED with <data> path
            matching), which is what lint's WearableBindListener check expects here instead.
            However, on this app's actual Play Services version, mixing BIND_LISTENER with those
            legacy per-action filters on the same service caused Play Services to onNullBinding()
            the service instead of ever invoking onMessageReceived - confirmed via real-device
            testing that removing the legacy filters and keeping only BIND_LISTENER is what
            actually fixed message delivery. Do not revert this to satisfy the lint warning
            without re-confirming fine-grained filters alone deliver messages reliably first.
            -->
            <intent-filter>
                <action
                    android:name="com.google.android.gms.wearable.BIND_LISTENER"
                    tools:ignore="WearableBindListener" />
            </intent-filter>
        </service>
        <service
            android:name=".services.G5CollectionService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="connectedDevice" />
        <service
            android:name=".services.Ob1G5CollectionService"
            android:enabled="true"
            android:foregroundServiceType="connectedDevice" />
        <service
            android:name=".services.DexCollectionService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="connectedDevice" />
        <service
            android:name=".services.DexShareCollectionService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="connectedDevice" />
        <service
            android:name=".utilitymodels.Notifications"
            android:exported="false" />
        <service
            android:name=".services.AlertVibrationService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />
        <service
            android:name=".services.SnoozeOnNotificationDismissService"
            android:exported="false" />
        <service
            android:name=".services.WearAlertBridgeDismissListener"
            android:exported="true"
            android:label="xDrip Alert Dismiss Listener"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
        <service
            android:name=".services.MissedReadingService"
            android:enabled="true"
            android:exported="true" />
        <service
            android:name=".services.CustomComplicationProviderService"
            android:exported="true"
            android:icon="@drawable/ic_watch_white"
            android:label="xDrip Complication Provider"
            android:permission="com.google.android.wearable.permission.BIND_COMPLICATION_PROVIDER">
            <intent-filter>
                <action android:name="android.support.wearable.complications.ACTION_COMPLICATION_UPDATE_REQUEST" />
            </intent-filter>

            <meta-data
                android:name="android.support.wearable.complications.SUPPORTED_TYPES"
                android:value="SHORT_TEXT,LONG_TEXT,RANGED_VALUE" />
            <meta-data
                android:name="android.support.wearable.complications.UPDATE_PERIOD_SECONDS"
                android:value="60" />
        </service>
        <service android:name=".services.HeartRateService" />
        <service
            android:name=".services.HeartRateJobService"
            android:permission="android.permission.BIND_JOB_SERVICE" />
        <service
            android:name=".ProcessAPKChannelDownload"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <receiver
            android:name=".NSEmulatorReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.eveningoutpost.dexdrip.NS_EMULATOR" />
                <action android:name="com.eveningoutpost.dexdrip.OOP2_DECODE_FARM_RESULT" />
            </intent-filter>
        </receiver>
        <receiver android:name=".services.ComplicationTapBroadcastReceiver" />
        <receiver
            android:name=".utils.DisconnectReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.bluetooth.device.action.ACL_DISCONNECTED" />
            </intent-filter>
        </receiver>
        <receiver android:name=".utils.framework.WakeLockTrampoline" />

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="913d7bd9c0b910d315c3bc0e057056daf73b698e" />

        <activity
            android:name=".NWPreferences"
            android:exported="true"
            android:icon="@drawable/ic_icon"
            android:label="@string/app_name"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Simulation"
            android:exported="true"
            android:icon="@drawable/ic_icon"
            android:label="@string/title_activity_simulation"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="com.google.android.voicesearch.SELF_NOTE" />

                <data android:mimeType="text/plain" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".KeypadInputActivity"
            android:exported="true"
            android:icon="@drawable/ic_icon"
            android:label="Keypad Treatment"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".QuickSnooze"
            android:exported="true"
            android:icon="@drawable/ic_icon"
            android:label="@string/snooze_alert"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LocationPermissionActivity"
            android:label="@string/title_activity_location_permission"
            android:launchMode="singleTop">

        </activity>
        <activity
            android:name=".SnoozeActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/snooze" />
        <activity
            android:name=".MenuActivity"
            android:configChanges="orientation|screenSize"
            android:label="Menu" />
        <activity
            android:name=".UpdateActivity"
            />
        <activity
            android:name=".tables.CalibrationDataTable"
            android:configChanges="orientation|screenSize"
            android:label="@string/calibration_data_table" />
        <activity
            android:name=".tables.BgReadingTable"
            android:configChanges="orientation|screenSize"
            android:label="@string/bg_readings_table" />
        <activity
            android:name=".tables.SensorDataTable"
            android:configChanges="orientation|screenSize"
            android:label="@string/sensor_table" />
        <activity
            android:name=".tables.BloodTestTable"
            android:configChanges="orientation|screenSize"
            android:label="@string/bg_readings_table" />
        <activity
            android:name=".tables.TreatmentsTable"
            android:configChanges="orientation|screenSize"
            android:label="@string/bg_readings_table" />
        <activity android:name=".SensorPermissionActivity" />

        <receiver

            android:name=".ConfigReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.gmail.jamorham.NEW_CONFIG" />
            </intent-filter>
        </receiver>
    </application>

</manifest>